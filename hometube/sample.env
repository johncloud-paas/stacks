JOHNCLOUD_ROOT=/path/to/root/of/paas/data
HOST=hometube.domain.tld
TZ=Etc/UTC
TRAEFIK_NETWORK=traefik-proxy
HOME_GROUP=Ressources
HOME_NAME=HomeTube
HOME_ICON=hometube.png
HOME_DESCRIPTION=Identity provider prioritizing security and control of your most sensitive data
# --- Languages ---
UI_LANGUAGE=en

# --- Audio Language Preferences ---
# Subtitles are automatically determined based on audio language preferences below
# Primary audio language preference (e.g., fr, en, es, de, ja)
LANGUAGE_PRIMARY=en

# Secondary audio languages (comma-separated, e.g., en,es,de)
LANGUAGES_SECONDARIES=fr

# Include subtitles for primary language
LANGUAGE_PRIMARY_INCLUDE_SUBTITLES=true

# Prioritize original voice (VO) before primary language
VO_FIRST=true

VIDEOS_FOLDER_DOCKER_HOST=$JOHNCLOUD_ROOT/sftpgo/data/ydethe/Videos/Shows/Youtube
YOUTUBE_COOKIES_FILE_PATH_DOCKER_HOST=$JOHNCLOUD_ROOT/metube/cookies/cookies.txt
TMP_DOWNLOAD_FOLDER_DOCKER_HOST=$JOHNCLOUD_ROOT/$COMPOSE_PROJECT_NAME/tmp

# --- yt-dlp ---
# Custom yt-dlp arguments (example: --proxy http://proxy:8080 --max-filesize 100M)
#YTDLP_CUSTOM_ARGS=--max-filesize 100M --write-info-json

# --- Jellyfin Integration ---
#JELLYFIN_BASE_URL=https://jellyfin.local:8096
#JELLYFIN_API_KEY=YOUR_JELLYFIN_API_KEY

# --- Quality & Download Preferences ---
# Download quality profiles to test. HomeTube proposes 4 main profiles. `auto`, by default, will strategically go through all of them and stop when success
#QUALITY_PROFILE=auto

# Maximum video resolution: "max" for highest available, or specific resolution
# Options: max, 2160, 1440, 1080, 720, 480, 360
#VIDEO_QUALITY_MAX=max

# Allow quality downgrade on profile failure (false = stop at first failure)
#QUALITY_DOWNGRADE=true

# Embed chapters and subtitles by default
#EMBED_CHAPTERS=true
#EMBED_SUBTITLES=true

# Video cutting precision mode
#CUTTING_MODE=keyframes

# --- Safety Options ---
# Allow overwriting existing videos in destination folder (false = protect existing files)
#ALLOW_OVERWRITE_EXISTING_VIDEO=false

# --- Advanced Options ---
# Remove temporary files after successful download (false = keep for debugging/reuse)
REMOVE_TMP_FILES_AFTER_DOWNLOAD=false

# Clean tmp folder before each new download (true = fresh start, useful after errors)
#NEW_DOWNLOAD_WITHOUT_TMP_FILES=false

# --- Browser Configuration ---
# Default browser for cookie extraction
#BROWSER_SELECT=chrome
